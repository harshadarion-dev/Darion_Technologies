/**
 * If we need any specific programmatic initialization for Carbon Web Components,
 * it happens here. For most CDS elements loaded via standard module scripts,
 * they self-initialize, but we expose this file just in case.
 */
document.addEventListener("DOMContentLoaded", () => {
    console.log("Darion Technologies UI Initialized.");
    // Wait for components to upgrade if needed
});
